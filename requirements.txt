# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.10.0
email-validator>=2.2.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.1

# LLM Providers
ollama>=0.3.0
openai>=1.54.0
anthropic>=0.34.0

# Vector Databases
chromadb>=0.4.18  # Production-ready vector database
# ChromaDB needs sqlite3 >= 3.35. On older OSes (RHEL 8 / CentOS) the system
# SQLite is too old, so this wheel bundles a modern SQLite the app swaps in
# (see backend/shared/vector_db.py). Harmless on systems that already have a new sqlite.
pysqlite3-binary>=0.5.0; sys_platform == "linux"
# Optional vector databases (for future use):
# qdrant-client==1.7.0  # Uncomment if you want to use Qdrant
# pinecone-client==2.2.4  # Uncomment if you want to use Pinecone

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
python-pptx==0.6.23
# Used only to pull embedded images out of PDFs/DOCX/PPTX for optional
# vision-based diagram description (see ENABLE_VISION_EXTRACTION in config.py).
pymupdf>=1.24.0
# Explicitly pinned even though pymupdf/chromadb pull it in transitively -
# don't want image-dimension checks to silently break on a future install.
pillow>=10.0.0
# Pure-Python text extraction for legacy Office (.doc/.ppt/.xls/.rtf), CSV,
# HTML, and OpenOffice (.odt/.odp/.ods) files - no LibreOffice/system
# dependency needed. See document_processor.extract_text_generic.
sharepoint-to-text>=1.1.0

# Database
sqlalchemy>=2.0.36
aiosqlite>=0.20.0

# Utilities
aiofiles>=24.1.0
httpx>=0.27.0

